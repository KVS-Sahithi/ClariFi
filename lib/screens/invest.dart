import 'package:flutter/material.dart';

class InvestScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Welcome to ClariFi Invest 👋', style: TextStyle(fontSize: 18)),
    );
  }
}
